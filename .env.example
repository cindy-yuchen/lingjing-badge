# DeepSeek API 密钥（必填）。
# 仅用于服务端（Cloudflare Pages Functions），绝不暴露到前端、也绝不写入仓库。
# 生产环境在 Cloudflare Pages 面板 → Settings → Environment variables 中配置为 Secret。
DEEPSEEK_API_KEY=""

# DeepSeek 模型名称（可选，默认 deepseek-v4-flash）。
# 若 deepseek-v4-flash 不可用，可改为 deepseek-chat 或 deepseek-reasoner。
DEEPSEEK_MODEL="deepseek-v4-flash"
